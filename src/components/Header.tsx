import React from "react";
import '../App.css';

const Header: React.FC = () => (
    <header className="header">
        <h1 className="heading">The Zoo</h1>
    </header>
);

export default Header;
